import { StyleSheet } from 'react-native';
import { theme } from 'config/theme';

export const styles = StyleSheet.create({
  containerStyle: {
    borderWidth: 1,
    borderColor: theme.switch.borderColor,
  },
});
